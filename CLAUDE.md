# Development Guidelines

## Lint Requirements (Deploy Rule)

**ALL lint errors and warnings must be fixed before commit/push.**

- ESLint: `cd web && npm run lint` must pass with 0 errors
- CSS Lint: `cd web && npm run lint:css` must pass with 0 errors
- Both lints must be clean before any commit or PR
