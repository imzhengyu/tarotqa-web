# TarotQA Web —— 依赖说明（注意：这不是 Python 依赖清单）
#
# 真正的依赖声明以 package.json / lock 文件为准：
#   web/package.json + web/pnpm-lock.yaml   前端应用（React + Vite）
#   package.json + pnpm-lock.yaml            仓库级脚本依赖
#   doc/package.json                         介绍 PPT 的构建依赖
#
# 安装：
#   pnpm --dir web install
#
# Python 侧没有第三方依赖：scripts/ 下的检查脚本只用标准库
# （run_checks.py / run_sanity.py / audit_assets.py / probe_astrology.py / checklog.py）。
# 例外：scripts/generate_mimi_tarot.py 需要 requests 与 Pillow，仅在生成 mimi 卡面时使用：
#   pip install requests Pillow
